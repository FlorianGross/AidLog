// Focused patient-capture page for ONE protocol of a deployment. Per-deployment,
// only reachable after unlock; it cannot be crawled/prerendered. The SPA
// `fallback: index.html` (adapter-static) serves it at runtime.
export const prerender = false;
