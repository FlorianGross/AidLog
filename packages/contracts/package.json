{
  "name": "@aidlog/contracts",
  "version": "0.1.0",
  "description": "Shared, framework-agnostic types and constants — the wire contract between crypto-core, api and web. No runtime dependencies.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests in contracts\" && exit 0",
    "lint": "echo \"lint handled at root\" && exit 0"
  }
}
